// Minimal test case
let x: f64 = 0.5;
print("x =", x);  // Should be 0.5
let result = x * 2;  
print("x * 2 =", result);  // Should be 1.0

// Test with explicit float literal
let y: f64 = 0.5 * 2.0;
print("0.5 * 2.0 =", y);  // Should be 1.0
