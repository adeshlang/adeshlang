fn main() {
    print("before unsafe block");

    unsafe {
        let ptr = alloc(64);
        print("allocated pointer:", ptr);
        free(ptr);
        print("freed pointer");
    }

    print("after unsafe block");
}
