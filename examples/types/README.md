# Type System Examples

This folder now uses one canonical showcase file:

- [test_numeric_literals.adesh](test_numeric_literals.adesh)

It covers numeric literal formats, float width inference, `type` and `typeof`, object syntax with numeric keys, `hasKey`, union/nullable flows, and control-flow narrowing edge cases.