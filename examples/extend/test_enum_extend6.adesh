enum Direction {
    North,
    South,
    East,
    West
}

extend DirectionTest on Direction {
    fn opposite():String {
       return match this.tag {
            North => "got North",
            South => "got South",
            _ => "other"
        };
    }
}

let d = Direction.North();
print("tag:", d.tag);
let result = d.opposite();
print("result:", result);
