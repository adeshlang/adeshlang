fn main() {
    // Scoped region for resource management
    {
        let header = [1, 2, 3];
        let payload = [4, 5, 6];
        print("region objects alive");
        print("header:", header);
        print("payload:", payload);
    }

    print("region freed; header and payload dropped");
}
