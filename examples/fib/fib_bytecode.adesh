@compile

fn fib(n) {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

let start = clock();
let result = fib(30);
let end = clock();

print("Fibonacci(30) =", result);
print("Time taken:", end - start, "seconds");
print("(bytecode mode)");
