let a = 10;
let b = 5;
let c = a * 2 + b / 5 - 3;
print("a:", a, "b:", b, "c:", c);
/*
 * This is a multi-line comment.
 * It can span multiple lines.
 */

 /*
 * This is a multi-line comment.
 * It can span multiple lines.
 */


 /** doc block  @param {number} x description  @returns {number} description  */
export let msg = "Hello" + " " + "AdeshLang!";
print(msg);
print(2+3);
