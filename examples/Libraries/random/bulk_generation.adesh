import Random;

print("--- Bulk Generation Demo ---");
let intBuf = Random.fillInts(5, 1, 10);
print("Bulk generated 5 ints in range [1, 10): " + intBuf);

let floatBuf = Random.fillFloats(4, 0.0, 1.0);
print("Bulk generated 4 floats in range [0, 1): " + floatBuf);
