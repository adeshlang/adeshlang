// Masked input example
// Prompts for a password without echoing characters
// input.mock(["secret123"]); // for non-interactive runs

let password = input("Password:", { masked: "*" });
print("Length: " + len(password));
print(`Password is ${password}`,{color:"#ff0000"});
